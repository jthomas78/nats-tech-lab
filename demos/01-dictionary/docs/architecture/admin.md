# Admin

<EyebrowLabel text="Architecture" />

The Admin UI's SYSTEM → NATS navbar group is a set of debugging/
observability panels — raw stream feed, KV buckets, cross-account
snapshots, trace inspection — layered over the same shared UI design
system the other lab frontends use.

<VerdictBadge status="completed" /> Running in code today.

*Content in progress — per-panel architecture and data-flow detail to
follow.*
