// Bundled default-locale (en) UI-copy catalog (BR-D11) — ships in the
// frontend build so UI chrome still renders if refdata-service is
// unreachable. Keys mirror the `ui-copy` dictionary type's seeded item
// codes (refdata-service/refdata/seed.go); refdata is an override layer on
// top of this catalog, not the other way round.
export const uiCopyFallbackEn = {
  'filter.all': 'All',
  'nav.language': 'Language',
}
